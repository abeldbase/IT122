<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete Car</title>
    <style>
      /* General styles */
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .content {
        flex: 1;
        padding: 20px;
        max-width: 800px;
        margin: 0 auto;
        background-color: #fff;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      .footer {
        background-color: #333;
        color: #fff;
        text-align: center;
        padding: 20px 0;
        position: fixed;
        bottom: 0;
        width: 100%;
      }

      h1 {
        color: #333;
        margin-top: 50px;
        font-size: 2em;
      }

      p {
        margin: 10px 0;
        font-size: 1.2em;
      }

      a {
        display: block;
        text-align: center;
        margin-top: 20px;
        text-decoration: none;
        color: #007bff;
        font-size: 1.1em;
      }

      a:hover {
        text-decoration: underline;
        color: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <h1>Delete Car</h1>
      <% if (deleted) { %>
      <p>Car with title "<%= title %>" has been deleted successfully.</p>
      <% } else { %>
      <p>Car with title "<%= title %>" was not found.</p>
      <% } %>
      <p>Total cars remaining: <%= total %></p>
      <a href="/">Go back to Home</a>
    </div>
    <footer class="footer">
      &copy; 2024 car inventory System
    </footer>
  </body>
</html>
